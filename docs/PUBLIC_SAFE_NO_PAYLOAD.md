# Public-safe no-payload note

This repo package deliberately excludes `payload/` so it can be used in a public GitHub repository without publishing QRG model archives. Upload the payload zip directly to `/workspace` on the RunPod instance.
